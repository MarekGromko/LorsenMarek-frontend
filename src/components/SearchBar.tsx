interface SearchBarProps{
    placeholder?: string;
    onSearch?: (hint: string)=>any
}

const SearchBar = ()=>{
    return (
        <div className="search-bar">
            
        </div>
    )
}

export default SearchBar;